# Production secrets file
# IMPORTANT: This file contains sensitive information and should be:
# 1. Never committed to version control
# 2. Stored securely (e.g., in a secret management system)
# 3. Encrypted at rest
# 4. Access-controlled

# Database credentials
DB_PASSWORD=your-production-database-password-minimum-32-characters
DATABASE_URL=postgresql://homegenie:your-production-database-password-minimum-32-characters@postgres:5432/homegenie

# Application secrets
JWT_SECRET=your-production-jwt-secret-key-minimum-32-characters-long-and-random

# Redis password
REDIS_PASSWORD=your-production-redis-password-minimum-16-characters

# SMTP configuration (if using email features)
SMTP_PASSWORD=your-smtp-app-specific-password

# External API keys (if needed)
# WEATHER_API_KEY=your-weather-api-key
# MAPS_API_KEY=your-maps-api-key
# PAYMENT_SECRET_KEY=your-payment-processor-secret-key

# Encryption keys for sensitive data
# ENCRYPTION_KEY=your-aes-256-encryption-key-32-bytes-base64-encoded

# OAuth secrets (if using OAuth authentication)
# GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
# GITHUB_CLIENT_SECRET=your-github-oauth-client-secret

# Webhook secrets (if using webhooks)
# WEBHOOK_SECRET=your-webhook-signing-secret

# Session secret (if using server-side sessions)
# SESSION_SECRET=your-session-encryption-secret-minimum-32-characters